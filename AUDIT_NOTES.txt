MAINTENANCE OS v1 - CONTRACTORS & PROPERTIES AUDIT REPORT
Generated: 2026-04-03

==============================================================================
SUMMARY
==============================================================================

Overall Assessment: SOLID IMPLEMENTATION with some notable gaps and UX considerations

Key Strengths:
- Complete CRUD operations for contractors and properties
- Robust validation and normalization (phone numbers, email)
- Archive/restore functionality for contractors
- Trade-based filtering for dispatch suggestions
- Timeline event logging for property actions
- Contractor stats calculations (response rates, completed jobs)

Areas for Improvement:
- Missing contractor edit page UI routing
- No contractor history/job tracking view from list
- No search/filter on contractors list
- Properties have no delete operation (can't delete archived properties)
- Duplicate contractor detection on update not enforced
- Email validation on edit could be stricter
- No bulk operations

==============================================================================
DETAILED FINDINGS
==============================================================================

CONTRACTORS MANAGEMENT
=====================

1. CONTRACTORS LIST (/app/(app)/contractors/page.tsx)
   Status: LOOKS_GOOD
   - Displays all active contractors in card grid layout
   - Shows contractor name, company, trade, and contact info
   - Displays performance metrics:
     * Completed jobs count
     * Number of times used
     * Response rate (when >= 3 dispatches)
     * Last contacted date
   - Has archive toggle button (Show Archived / Show Active)
   - Archive view shows restore buttons for each contractor
   - Responsive grid layout (md:grid-cols-2 lg:grid-cols-3)

2. ADD CONTRACTOR FORM (/app/(app)/contractors/new/page.tsx)
   Status: LOOKS_GOOD
   - Required fields: Name, Trade
   - Optional fields: Company Name, Phone, Email, Notes, Preferred checkbox
   - Form validation present
   - Error handling with user-friendly messages
   - Auto-redirect to contractors list on success

3. EDIT CONTRACTOR FORM (/app/(app)/contractors/[id]/edit/page.tsx)
   Status: LOOKS_GOOD
   - Loads contractor data on mount
   - All fields editable: name, company, trade, phone, email, notes, preferred
   - Same validation as create form
   - Cancel button returns to previous page
   - Loading states for both data fetching and form submission

4. CONTRACTOR DETAIL PAGE (/app/(app)/contractors/[id]/page.tsx)
   Status: LOOKS_GOOD
   - Shows complete contractor information
   - Displays trade badge and preferred status
   - Shows phone/email as clickable links (tel: and mailto:)
   - Activity card with:
     * Active jobs count
     * Completed jobs count
     * Total dispatches sent
     * Date added
   - Edit button links to edit page
   - Archive button with confirmation dialog
   - Error message display in absolute position (UI_ISSUE: may overlap content)

5. CONTRACTOR API - POST /api/contractors/route.ts
   Status: LOOKS_GOOD
   - Validation: name (1-200 chars), trade enum, optional fields
   - Phone validation: requires >= 10 digits, normalizes to +1XXXXXXXXXX format
   - Email validation: standard email format, max 254 chars
   - Duplicate check: prevents same email/phone for same owner
   - Either phone or email required (enforced at API)
   - Phone normalization handles: 10-digit, 11-digit (starting with 1), international
   - Returns 409 Conflict if duplicate exists

6. CONTRACTOR API - GET /api/contractors/route.ts
   Status: LOOKS_GOOD
   - Retrieves all non-archived contractors for user
   - Optional stats mode (?stats=1)
   - Stats include: dispatches, response rates, completed jobs, avg response time
   - Proper authorization checks

7. CONTRACTOR API - PATCH /api/contractors/[id]/route.ts
   Status: BUG - Missing duplicate check on update
   - Can update all fields (name, company, trade, phone, email, notes, preferred, archived)
   - Phone/email normalization applied
   - BUT: Does NOT check for duplicate email/phone on update
   - Could create duplicate entries if user edits email/phone
   - Severity: LOW-MEDIUM (requires user to intentionally create duplicate)

8. CONTRACTOR API - DELETE /api/contractors/[id]/route.ts
   Status: LOOKS_GOOD
   - Actually archives contractor (sets isArchived=true, not DELETE)
   - Blocks archiving if contractor has active jobs
   - Error message specifies number of active jobs blocking operation
   - Proper authorization checks

9. RESTORE BUTTON (/app/(app)/contractors/restore-button.tsx)
   Status: LOOKS_GOOD
   - Simple restore UI in archived view
   - Uses PATCH with isArchived=false
   - Loading state management
   - Error alerts shown to user
   - Full-width button in card layout

10. ARCHIVE BUTTON (/app/(app)/contractors/[id]/archive-button.tsx)
    Status: UX_ISSUE
    - Uses window.confirm() for confirmation (old-style UX)
    - Error display positioned absolutely (can overlap content)
    - Should use AlertDialog like property deletion does
    - Error message styling inconsistent with rest of app

MISSING FEATURES - CONTRACTORS:

A) CONTRACTOR SEARCH/FILTER
   Status: MISSING_FEATURE
   - Contractors list has no search by name, company, or trade
   - Harder to find contractors in large lists
   - Could filter by trade, preferred status
   - Suggested: Add search input + filter dropdown on contractors page

B) CONTRACTOR HISTORY VIEW
   Status: MISSING_FEATURE
   - No way to see all issues a contractor was dispatched to from contractors list
   - Click on contractor goes to detail page, but no issues linked view
   - Could show recent dispatches, jobs they were selected for
   - Current workaround: Filter issues list manually

C) CONTRACTOR-PROPERTY LINKING
   Status: LOOKS_GOOD
   - PropertyContractor junction table exists and is used
   - Can link/unlink contractors to properties in property detail
   - But: Contractors list doesn't show which properties they're linked to
   - Minor: Would be useful for property-specific contractors

==============================================================================

PROPERTIES MANAGEMENT
=====================

1. PROPERTIES LIST (/app/(app)/properties/page.tsx)
   Status: LOOKS_GOOD
   - Shows all properties for user
   - Displays: nickname/address, property type, issue status summary
   - Shows counts: open issues, awaiting quotes, ready to review, active jobs
   - Color-coding for important statuses (green for ready, blue for active)
   - Click navigates to issues filtered by property
   - Empty state with call-to-action

2. ADD PROPERTY FORM (/app/(app)/properties/new/page.tsx)
   Status: LOOKS_GOOD
   - Required fields: Address Line 1, City, State, ZIP, Property Type
   - Optional: Nickname, Address Line 2
   - Property type dropdown with 6 options (single family, condo, apartment, townhouse, duplex, other)
   - Form validation
   - Error handling

3. EDIT PROPERTY PAGE (/app/(app)/properties/[id]/edit/page.tsx)
   Status: LOOKS_GOOD
   - Loads property data via API on mount
   - All fields editable
   - Loading state shown while fetching
   - Form state managed with controlled inputs
   - Save/Cancel buttons
   - Error handling with user messaging

4. PROPERTY DETAIL PAGE (/app/(app)/properties/[id]/page.tsx)
   Status: LOOKS_GOOD
   - Shows property details (type, full address)
   - Linked contractors section:
     * Lists all contractors linked to property
     * Shows remove button for each
     * Can add new contractors from available list
   - Tenant intake link management:
     * Generate new link
     * Deactivate existing link
     * Copy link to clipboard
   - Issues section:
     * Lists all issues for property
     * Shows title, description (truncated)
     * Status and urgency badges
     * Click navigates to issue detail
   - Edit and Delete buttons in header

5. PROPERTY API - POST /api/properties/route.ts
   Status: LOOKS_GOOD
   - Validation: all address fields required, type enum, nickname optional
   - Creates timeline event on creation
   - Proper user authorization

6. PROPERTY API - GET /api/properties/[id]/route.ts
   Status: LOOKS_GOOD
   - Retrieves single property with user authorization check
   - Returns 404 if not found or unauthorized

7. PROPERTY API - PATCH /api/properties/[id]/route.ts
   Status: LOOKS_GOOD
   - All fields optional for update
   - Creates timeline event on update
   - Includes payload in timeline (what was changed)
   - Proper authorization checks

8. PROPERTY API - DELETE /api/properties/[id]/route.ts
   Status: LOOKS_GOOD
   - Blocks deletion if property has active issues
   - Creates timeline event before deletion
   - Returns specific error message about active issues
   - Proper authorization checks

9. PROPERTY DETAIL CLIENT (/app/(app)/properties/[id]/property-detail-client.tsx)
   Status: LOOKS_GOOD
   - Handles delete, unlink, addContractor, intakeLink actions
   - Uses AlertDialog for confirmations (consistent UX)
   - Delete: confirms and checks for active issues
   - Unlink: removes contractor from property
   - Add Contractor: dropdown select + button
   - Intake Link: generate/deactivate/copy to clipboard
   - Error handling throughout

PROPERTIES ISSUES:

1. NO PROPERTY SEARCH/FILTER (MISSING_FEATURE)
   - List not searchable by nickname or address
   - Could add search box to filter list
   - Helpful for users with many properties

2. NO PROPERTY DELETION VIA UI (UX_ISSUE)
   - Delete button not exposed in property detail UI
   - API supports deletion (with safety checks)
   - User must have active issues to prevent deletion
   - Suggested: Show delete button only when safe to delete

3. ARCHIVED PROPERTIES NOT SUPPORTED (MISSING_FEATURE)
   - No archive/restore for properties like contractors
   - Can't view deleted properties (cascading delete removes all data)
   - Properties with completed issues are permanently deleted
   - Suggested: Add soft-delete (archive) instead of hard delete

==============================================================================

CROSS-CUTTING CONCERNS
=====================

CONTRACTOR FILTERING BY TRADE:
   Status: LOOKS_GOOD (in dispatch context)
   - Dispatch page (issues/[id]/dispatch) intelligently filters contractors
   - Uses issue.category to map to relevant trades
   - Shows trade-matched contractors first
   - Falls back to all contractors if none match
   - Preferred contractors sorted to top
   - Trade mapping defined in dispatch page (tradeMap)

VALIDATION & NORMALIZATION:
   Status: LOOKS_GOOD
   - Phone numbers: Normalized to international format (+1XXX...)
   - Email: Lowercased and trimmed
   - Length limits: Name (200), Email (254), Notes (2000), Phone (30)
   - Trade enum validation on create/update
   - Either phone or email required on create
   - BUT: Duplicate check only on create, not on update (see issue #7)

DATA INTEGRITY:
   Status: LOOKS_GOOD
   - Cascade deletes configured in Prisma (Property, Issue, Dispatch deletes cascade)
   - Foreign key constraints via Prisma relations
   - User ownership enforced on all queries
   - Timeline events logged for property changes

TIMELINE/AUDIT:
   Status: LOOKS_GOOD (for properties)
   - Property creation/update/deletion logged
   - No timeline events for contractor changes (MISSING_FEATURE)
   - Timeline events useful for tracking property history

==============================================================================

FORM & UI ISSUES
=====================

1. ARCHIVE BUTTON ERROR POSITIONING (UX_ISSUE - Severity: LOW)
   Location: /app/(app)/contractors/[id]/archive-button.tsx:51
   - Error message positioned absolutely at top-right
   - Can overflow/overlap page content
   - Should use AlertDialog or inline error like other forms
   - Recommendation: Use alert() or AlertDialog for consistency

2. ADD CONTRACTOR FORM IN DISPATCH (UX_ISSUE - Severity: LOW)
   Location: /app/(app)/issues/[id]/dispatch/page.tsx:172-216
   - Inline form to add contractor while dispatching
   - Quick add feature is useful
   - Form validation basic (email regex check)
   - No phone normalization in inline form (but API handles it)

3. CONTRACTOR DETAIL EDIT BUTTON NOT PROMINENT (UX_ISSUE - Severity: LOW)
   - Edit button exists but small in header
   - Users might miss edit option
   - Could be more discoverable (move to top, better styling)

==============================================================================

MISSING OPERATIONS / GAPS
=====================

1. CONTRACTOR BULK OPERATIONS (NICE_TO_HAVE)
   - No bulk archive/restore
   - No bulk add to property
   - Would be useful for power users

2. CONTRACTOR EXPORT (NICE_TO_HAVE)
   - No CSV/Excel export of contractor list
   - Would help with reporting

3. PROPERTY SOFT DELETE / ARCHIVE (MISSING_FEATURE)
   - Properties hard-deleted with cascade
   - No way to undelete
   - Suggests implementing archive pattern like contractors

4. CONTRACTOR BULK PROPERTY LINKING (NICE_TO_HAVE)
   - Can only link one contractor at a time to property
   - No way to link multiple contractors in bulk

5. CONTRACTOR PERFORMANCE DASHBOARD (NICE_TO_HAVE)
   - Stats exist but not easily viewable
   - Could show response times, job completion rates
   - Currently only visible in list cards and dispatch page

==============================================================================

SECURITY NOTES
=====================

1. AUTHORIZATION: Properly implemented
   - All queries check ownerUserId
   - No cross-user data leakage possible

2. VALIDATION: Solid
   - Zod schemas on all endpoints
   - Phone/email normalization
   - Trade enum enforcement

3. DUPLICATE PREVENTION: Partial
   - Duplicates prevented on CREATE
   - NOT prevented on UPDATE (potential issue)

4. RATE LIMITING: Not visible in code
   - No rate limiting on contractor/property endpoints
   - Could be implemented at middleware level

==============================================================================

DATABASE SCHEMA REVIEW
=====================

Contractor Model:
- Required: id, ownerUserId, name, trade
- Optional: companyName, phone, email, notes
- Boolean: isPreferred (default false), isArchived (default false)
- Relations: owner (User), propertyLinks (PropertyContractor[]), dispatches (Dispatch[]), jobs (Job[]), contractorMessages (ContractorMessage[])
- ASSESSMENT: Schema is complete and well-designed

Property Model:
- Required: id, ownerUserId, addressLine1, city, state, postalCode, propertyType
- Optional: nickname, addressLine2
- Relations: owner (User), issues (Issue[]), propertyLinks (PropertyIntakeLink[]), propertyWorkers (PropertyContractor[]), timelineEvents (TimelineEvent[])
- ASSESSMENT: Schema is complete. Missing: isArchived flag (if soft delete desired)

PropertyContractor Junction:
- Links contractors to properties
- Unique constraint on (propertyId, contractorId)
- Cascade delete on both sides
- ASSESSMENT: Good design for many-to-many relationship

==============================================================================

PRIORITIZED FINDINGS SUMMARY
=====================

CRITICAL BUGS:
  None identified

BUGS:
  1. PATCH /api/contractors/[id] - Missing duplicate email/phone check on update
     Severity: MEDIUM
     Impact: Could create duplicate contractors
     Fix: Add same duplicate check logic as POST route

UX ISSUES:
  1. Archive button error positioning - overlaps content
     Severity: LOW
     Fix: Use AlertDialog or inline error message

  2. Contractor edit page link exists but not exposed in list
     Severity: LOW
     Fix: Add edit button to contractor cards

  3. No property delete button in UI (API supports it)
     Severity: LOW
     Fix: Show delete button when safe (no active issues)

MISSING FEATURES:
  1. Contractor search/filter on list page
     Severity: MEDIUM (impacts usability with many contractors)
     Effort: LOW
     Value: HIGH

  2. Property soft delete / archive
     Severity: LOW
     Effort: MEDIUM
     Value: MEDIUM

  3. Contractor timeline/history
     Severity: LOW
     Effort: LOW
     Value: MEDIUM

  4. Property search/filter
     Severity: LOW
     Effort: LOW
     Value: MEDIUM

NICE TO HAVE:
  1. Bulk operations (archive, link to property)
  2. Contractor/property export (CSV/Excel)
  3. Contractor performance dashboard
  4. Rate limiting on endpoints
  5. Contractor history view from list

==============================================================================

TESTING RECOMMENDATIONS
=====================

1. Test contractor duplicate prevention on UPDATE (should fail currently)
2. Test archive with active jobs (should block)
3. Test property deletion with active issues (should block)
4. Test phone number normalization with various formats
5. Test contractor stats calculations with edge cases
6. Test cross-user data isolation (user A can't access user B's contractors)
7. Test intake link generation and deactivation
8. Test contractor-property linking and unlinking
9. Test search/filter once implemented
10. Test error handling and messaging in all flows

==============================================================================
