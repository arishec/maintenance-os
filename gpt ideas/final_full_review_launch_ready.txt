MAINTENANCE OS
FINAL FULL REVIEW (CODE + UX + PRICING + SEO)
LAST CORRECTIONS BEFORE LAUNCH

==================================================
FINAL VERDICT
==================================================

You are now:

LAUNCHABLE

Not “almost”
Not “prototype”

This is a real product.

What remains are:
- small UX polish items
- a few logic hardening items
- wording improvements

Nothing here blocks launch.

==================================================
CRITICAL (FIX BEFORE LAUNCH)
==================================================

1. DOMAIN CONSISTENCY (SEO + STRIPE)

Make sure ONE domain is used everywhere:

CHECK:
- NEXT_PUBLIC_SITE_URL
- metadataBase
- sitemap.ts
- robots.ts
- Stripe success/cancel URLs
- JSON-LD URLs

RULE:
Never hardcode a fallback domain like "ifbids.com"

USE:

const siteUrl = process.env.NEXT_PUBLIC_SITE_URL!

--------------------------------------------------

2. PRISMA + BUILD SYNC

Run:

npm install
npx prisma generate
npx prisma migrate dev
npx tsc --noEmit

If ANY TS errors exist:
Fix them before launch.

--------------------------------------------------

3. STRIPE WEBHOOK HARDENING

Current issue:
Relies too heavily on metadata.userId

FIX:

Add fallback lookup:

- by stripeSubscriptionId
- by stripeCustomerId

This prevents silent failures.

--------------------------------------------------

4. ALREADY-PRO GUARD

In checkout route:

Add:

if (user.plan === 'pro') {
  return error
}

Prevents duplicate subscriptions.

--------------------------------------------------

5. PAYWALL ERROR HANDLING

Ensure frontend handles:

{ error: 'PAYWALL_REQUIRED' }

Correct behavior:
- open modal
- preserve state
- no crash

==================================================
HIGH VALUE (DO NOW IF POSSIBLE)
==================================================

6. HOMEPAGE HEADLINE

Replace with:

Stop chasing contractors and losing track of repairs

--------------------------------------------------

7. SUBHEADLINE

Use:

Report issues, send them to contractors, compare responses, and keep everything organized in one place.

--------------------------------------------------

8. CTA

Primary:

Start free — no credit card

Secondary:

See how it works

--------------------------------------------------

9. CORE VALUE SECTION

Add:

Send one issue. Get multiple responses.

This is your strongest selling point.

--------------------------------------------------

10. REMOVE WEAK LANGUAGE

Avoid:
- “manage everything”
- “all in one place” (overused)

Be specific:
- contractors
- quotes
- repairs

==================================================
PRODUCT LOGIC (CONFIRM FINAL)
==================================================

FREE:
- 3 issues total
- full history allowed
- NO dispatch

PRO:
- unlimited issues
- dispatch enabled

This is correct.

--------------------------------------------------

IMPORTANT:
You are using:
3 issues TOTAL (lifetime)

Confirm you want this.

Do NOT accidentally message:
“3 per month”

==================================================
UX POLISH (NON-BLOCKING)
==================================================

11. NAV ACTIVE STATES

Highlight current page in top nav.

--------------------------------------------------

12. LOGO INTERACTION

Add hover + pointer:

hover:opacity-80
cursor-pointer

--------------------------------------------------

13. REMOVE DASHBOARD DUPLICATION

Do not show "Dashboard" in top nav if already in sidebar.

(You already fixed this — just confirming)

--------------------------------------------------

14. HEADER SPACING

Change:

px-6 → px-8

Cleaner alignment with layout.

==================================================
PRICING PAGE (FINAL CHECK)
==================================================

REMOVE:
“unlimited SMS”

REPLACE WITH:
“SMS & email contractor dispatch”

Reason:
protects you from abuse cost.

==================================================
STRIPE FINAL CHECKLIST
==================================================

✔ customer created or reused
✔ customer ID stored on user
✔ subscription ID stored
✔ webhook updates plan
✔ cancellation downgrades plan
✔ success URL correct
✔ cancel URL correct

==================================================
TEST BEFORE LAUNCH (DO THIS EXACTLY)
==================================================

TEST 1:
Free user creates 3 issues → works

TEST 2:
Free user creates 4th → paywall triggers

TEST 3:
Free user clicks dispatch → paywall triggers

TEST 4:
Upgrade → Stripe checkout works

TEST 5:
Webhook fires → user.plan = pro

TEST 6:
Cancel subscription → user.plan = free

TEST 7:
Pro user cannot re-buy plan

==================================================
WHAT YOU SHOULD NOT TOUCH ANYMORE
==================================================

Do NOT:
- add more features
- add landlord tier yet
- redesign layout again
- over-optimize performance
- overcomplicate pricing

==================================================
FINAL TRUTH
==================================================

You are no longer building.

You are now:

READY TO SHIP

Everything remaining is polish and real-user feedback.

==================================================
FINAL ANSWER
==================================================

There are NO major architectural problems left.

Only:
- small correctness fixes
- polish

Ship it after:
- domain consistency
- prisma sync
- webhook fallback

==================================================
END OF DOCUMENT
