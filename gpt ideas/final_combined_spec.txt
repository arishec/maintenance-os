MAINTENANCE OS
ADMIN LOOKUP + BACKEND DEBUGGING + SYSTEM POLISH SPEC (COMBINED)

==================================================
PURPOSE
==================================================

This document combines:
1. Admin lookup endpoint hardening
2. Backend system validation (based on real output)
3. Final polish actions to make system production-ready

This is a COMPLETE implementation + refinement guide.

==================================================
SECTION 1 — ADMIN LOOKUP (HARDENED)
==================================================

Endpoint (rename from /api/admin/lookup):

/api/internal/lookup?ref=ISS-XXXXXX
/api/internal/lookup?ref=MNT-XXXXXX

--------------------------------------------------
SECURITY (REQUIRED)
--------------------------------------------------

1. Admin email check:

if (!user || !ADMIN_EMAILS.includes(user.email)) {
  return new Response("Forbidden", { status: 403 })
}

2. Add secret header:

if (req.headers.get("x-admin-secret") !== process.env.ADMIN_SECRET) {
  return new Response("Forbidden", { status: 403 })
}

--------------------------------------------------
REFERENCE HANDLING
--------------------------------------------------

if (ref.startsWith("ISS-")) {
  // issue lookup
} else if (ref.startsWith("MNT-")) {
  // dispatch lookup
} else {
  return {
    error: "INVALID_REFERENCE",
    message: "Reference must start with ISS- or MNT-"
  }
}

--------------------------------------------------
NOT FOUND RESPONSE
--------------------------------------------------

return {
  error: "NOT_FOUND",
  message: `No issue or dispatch found for ${ref}`
}

--------------------------------------------------
LOGGING
--------------------------------------------------

console.log("ADMIN_LOOKUP", {
  ref,
  user: user.email,
  timestamp: new Date()
})

--------------------------------------------------
SUMMARY BLOCK (REQUIRED)
--------------------------------------------------

Return:

{
  "type": "issue",
  "reference": "ISS-XXXXXX",
  "summary": {
    "status": "...",
    "contractorsContacted": X,
    "responses": X,
    "selectedContractor": "...",
    "jobStatus": "..."
  },
  "data": { ...full object }
}

==================================================
SECTION 2 — BACKEND SYSTEM POLISH (CRITICAL)
==================================================

Your system works end-to-end.
Now fix clarity + signal issues.

--------------------------------------------------
1. ADD SUMMARY TO ALL DEBUG OUTPUT
--------------------------------------------------

Problem:
Huge JSON = hard to read

Fix:
Always include summary block (see above)

--------------------------------------------------
2. NORMALIZE TIMELINE EVENTS
--------------------------------------------------

Replace inconsistent events with:

eventType:
- issue_created
- dispatch_sent
- contractor_replied
- owner_replied
- contractor_selected
- job_created
- job_scheduled
- job_completed

--------------------------------------------------
3. DEDUPE TIMELINE EVENTS
--------------------------------------------------

Problem:
Repeated "owner_reply_sent"

Fix:
- prevent duplicate inserts
OR
- group events when rendering

--------------------------------------------------
4. FIX requiresReview LOGIC
--------------------------------------------------

Current:
requiresReview = true even when parsed

Fix:

if (price && availability && message) {
  requiresReview = false
}

--------------------------------------------------
5. IMPROVE EXTRACTION CONFIDENCE
--------------------------------------------------

Current:
0.6 even when successful

Fix:

if (price && availability) confidence = 0.85+
if (price only) confidence = 0.7

--------------------------------------------------
6. FIX JOB CREATION FLOW
--------------------------------------------------

Current:
Job instantly "scheduled"

Fix lifecycle:

contractor_selected
→ job_created (status: pending)
→ job_scheduled (date assigned)

--------------------------------------------------
7. FIX NOTIFICATIONS PIPELINE
--------------------------------------------------

Problem:
Notifications exist but not shown

Fix:
- ensure UI fetches notifications
OR
- remove notifications system until used

--------------------------------------------------
8. CLEAN RAW MESSAGE STORAGE
--------------------------------------------------

Current:
Too much duplication

Fix:
- store latest reply cleanly
- optionally trim email thread history

==================================================
SECTION 3 — FINAL PRODUCT SAFETY ITEMS
==================================================

These prevent real-world failure.

--------------------------------------------------
1. MANUAL QUOTE ENTRY
--------------------------------------------------

Add button:
"Add quote manually"

Fields:
- contractor
- price
- availability
- notes

--------------------------------------------------
2. RESEND / RETRY DISPATCH
--------------------------------------------------

Add:
- "Resend request"
- "Send to another contractor"

--------------------------------------------------
3. SIMPLE ACTIVITY TIMELINE (UI)
--------------------------------------------------

Show:

- Sent to 3 contractors
- Mike replied
- You replied
- Contractor selected

--------------------------------------------------
4. NEXT STEP GUIDANCE
--------------------------------------------------

Status → guidance:

Awaiting quotes → "Waiting for replies"
Quotes received → "Select a contractor"
Scheduled → "Confirm completion"

--------------------------------------------------
5. CONTRACTOR DEDUPLICATION
--------------------------------------------------

Normalize:
- phone numbers
- email lowercase

Prevent duplicates

==================================================
FINAL VERDICT
==================================================

You have:

- working system
- full lifecycle
- real communication layer
- strong debugging capability

You need:

- clarity (summary + timeline)
- correctness (requiresReview, confidence)
- structure (job lifecycle)
- resilience (manual + retry)

==================================================
FINAL TRUTH
==================================================

This system is no longer fragile.

You are now refining:
→ clarity
→ signal
→ real-world behavior handling

That is the final stage before production.

END OF DOCUMENT
